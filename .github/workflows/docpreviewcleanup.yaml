name: Doc Preview Cleanup

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  doc-preview-cleanup:
    uses: EpiAware/.github/.github/workflows/docs-preview-cleanup.yml@1b36d466be7e132c3a8a4f75d9ac90d9642c6f81
    secrets: inherit  # pragma: allowlist secret
