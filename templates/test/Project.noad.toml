# PACKAGE-OWNED — scaffold writes this once and never overwrites it.
# The test environment (no-AD variant: the package opted out of the AD harness,
# so the AD deps are absent). Seeded with the deps the shared QA helpers need;
# add the package's own test-only deps here. EpiAwarePackageTools supplies the
# helpers; it is registered, so it is bounded in `[compat]`.
[deps]
Aqua = "4c88cf16-eb10-579e-8560-4a9242c79595"
BenchmarkTools = "6e4b80f9-dd63-53aa-95a3-0cdb28fa8baf"
Documenter = "e30172f5-a6a5-5a46-863b-614d45cd2de4"
{{KIT_DEP_LINE}}ExplicitImports = "7d51a73a-1435-4ff3-83d9-f097790105c7"
JET = "c3a54625-cd67-489e-a8e7-0a5a0ff4e31b"
Pkg = "44cfe95a-1eb2-52ea-b672-e2afdf69b78f"
Runic = "62bfec6d-59d7-401d-8490-b29ee721c001"
Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"
TestItemRunner = "f8b46487-2199-4994-9208-9a1283c18c0a"
TestItems = "1c621080-faea-4a02-84b6-bbd5e436b8fe"
{{PACKAGE}} = "{{UUID}}"

# Bounded compat for the seeded QA deps. The package under test is pinned
# by `[sources]`, so carries no compat entry.
# Add a bound for each test-only dep you add. No-AD variant: no AD-harness
# deps appear.
# TestItemRunner is capped to 1.1.x: 1.2 breaks `run_package_tests`.
[compat]
Aqua = "0.8"
BenchmarkTools = "1"
Documenter = "1"
{{KIT_COMPAT_LINE}}ExplicitImports = "1.6"
JET = "0.9, 0.10"
Pkg = "1"
Runic = "1"
Test = "1"
TestItemRunner = "1.1 - 1.1"
TestItems = "1"

[sources]
{{PACKAGE}} = {path = ".."}
