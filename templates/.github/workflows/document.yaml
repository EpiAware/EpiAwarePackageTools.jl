# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Documenter

on:
  push:
    branches: main
    tags: '*'
  pull_request:
  merge_group:
  release:
    types: [published]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  Documenter:
    uses: {{ORG}}/.github/.github/workflows/documentation.yml@5a39aa9a129edc837ecd5c59f29885fe4c7da464
{{DOCS_TIMEOUT_WITH}}    secrets: inherit  # pragma: allowlist secret
