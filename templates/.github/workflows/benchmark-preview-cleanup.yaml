# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Benchmark Preview Cleanup

# Clears a closed PR's `ad/previews/PR<N>/` directory from the `benchmarks`
# branch (#443), mirroring `docpreviewcleanup.yaml` for `gh-pages`. Only a PR
# that ran the AD bench leg (a `benchmark` label, or a manual dispatch) ever
# has a preview directory to remove; the reusable is a no-op otherwise.

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  benchmark-preview-cleanup:
    uses: {{ORG}}/.github/.github/workflows/benchmark-preview-cleanup.yml@42a0501ccacefbfc2f2eeca640714a19c50bfe58
    secrets: inherit  # pragma: allowlist secret
