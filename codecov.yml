# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
# Coverage rolls up under a single `unit` flag. No-AD variant (no per-backend
# `ad-*` flags); see `codecov.yml` for the AD-enabled variant.
#
# `carryforward: true` keeps the flag's last value when a run doesn't
# re-upload it, matching the AD-enabled variant.
flags:
  unit:
    paths:
      - src
      - ext
    carryforward: true
