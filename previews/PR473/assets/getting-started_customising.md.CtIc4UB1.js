import{_ as t,o as a,c as o,an as s}from"./chunks/framework.DgP4Bj_d.js";const u=JSON.parse('{"title":"Customising your docs","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/customising.md","filePath":"getting-started/customising.md","lastUpdated":null}'),n={name:"getting-started/customising.md"};function i(r,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="customising" tabindex="-1">Customising your docs <a class="header-anchor" href="#customising" aria-label="Permalink to &quot;Customising your docs {#customising}&quot;">​</a></h1><p><code>scaffold</code> seeds a starting set of docs source pages once; after that they belong to the adopting package, not to the kit. This page explains which files are yours to rewrite and where to start.</p><h2 id="What&#39;s-package-owned-here" tabindex="-1">What&#39;s package-owned here <a class="header-anchor" href="#What&#39;s-package-owned-here" aria-label="Permalink to &quot;What&#39;s package-owned here {#What&#39;s-package-owned-here}&quot;">​</a></h2><p><code>update</code> never rewrites these, no matter how many times it runs:</p><ul><li><p><code>getting-started/index.md</code> — the quickstart a new user lands on first.</p></li><li><p><code>getting-started/infrastructure.md</code> and any further page added under <code>docs/src/</code>.</p></li><li><p><code>docs/pages.jl</code> — the navigation tree; add, remove, or reorder entries freely.</p></li><li><p>The README body (the badge block and the standard sections between their managed markers are rewritten on sync; everything else is package-owned).</p></li><li><p><code>CITATION.cff</code> — your citation metadata (authors, DOI, version). Seeded once so GitHub renders a &quot;Cite this repository&quot; widget, then never rewritten; edit it as the package is released.</p></li><li><p><code>docs/src/extensions/*.md</code> — one page per package extension, seeded from the <code>[extensions]</code> your <code>Project.toml</code> declared at scaffold time. See <a href="/previews/PR473/getting-started/customising#documenting-extensions">Documenting extensions</a> below.</p></li></ul><p>See <a href="/previews/PR473/getting-started/infrastructure#infrastructure">Infrastructure and template sync</a> for the full managed-versus-package-owned breakdown.</p><h2 id="Making-it-your-own" tabindex="-1">Making it your own <a class="header-anchor" href="#Making-it-your-own" aria-label="Permalink to &quot;Making it your own {#Making-it-your-own}&quot;">​</a></h2><ul><li><p>Replace the seeded quickstart in <code>getting-started/index.md</code> with the package&#39;s real installation steps and a runnable first example.</p></li><li><p>Add new pages under <code>docs/src/</code> as the package grows (tutorials, guides, worked examples), then list them in <code>docs/pages.jl</code>.</p></li><li><p>Reorder or rename any <code>Getting started</code> entry; <code>pages.jl</code> is read fresh on every <code>docs/make.jl</code> run, so there is no drift to fight.</p></li><li><p>Keep or delete this page once it has served its purpose — it is package-owned like the rest of <code>getting-started/</code>.</p></li></ul><p>The kit&#39;s own copies of these pages (the ones this documentation site renders) are the worked example: this site is a real adopter of the scaffold, seeded once and then hand-edited the same way any adopting package&#39;s docs would be.</p><h2 id="documenting-extensions" tabindex="-1">Documenting extensions <a class="header-anchor" href="#documenting-extensions" aria-label="Permalink to &quot;Documenting extensions {#documenting-extensions}&quot;">​</a></h2><p>A package that declares <code>[extensions]</code> gets an <code>Extensions</code> group in its nav when it is scaffolded, one entry per extension, each pointing at a seeded page under <code>docs/src/extensions/</code>. The entry is labelled with the weakdep that triggers the extension, so a reader sees <code>Plots</code>, not <code>MyPackagePlotsExt</code>.</p><p>Each page is yours: write what the extension adds and what a reader has to load to get it. The public-API block is seeded inert, shown as a code sample rather than run, because an extension module exists only once its weakdeps are loaded — a live <code>@autodocs</code> block would fail the docs build of a package whose docs environment does not carry them. To turn it on, add the weakdep to <code>docs/Project.toml</code>, add the extension module to <code>EXTRA_MODULES</code> in <code>docs/docs_config.jl</code>, and replace the outer block with the one inside it.</p><p><code>docs/pages.jl</code> is written once, so an extension added after scaffolding needs its page and its one nav line by hand — as a package does today when it turns benchmarks on. A build drops any <code>Extensions</code> entry whose page is missing, so a nav never carries a dangling link, and the group disappears entirely once nothing is left in it.</p><h2 id="tutorial-environments" tabindex="-1">Tutorials with their own environment <a class="header-anchor" href="#tutorial-environments" aria-label="Permalink to &quot;Tutorials with their own environment {#tutorial-environments}&quot;">​</a></h2><p>Every tutorial resolves against the shared <code>docs/</code> environment by default. Some cannot. A dependency may cap a shared package below the version your own extension needs, leaving the two unable to co-resolve at all.</p><p><code>TUTORIAL_ENVIRONMENTS</code> in <code>docs/docs_config.jl</code> opts one heavy tutorial out, as a <code>&quot;file.jl&quot; =&gt; &quot;environment/dir&quot;</code> pair. The directory is relative to <code>docs/</code> unless absolute, and the tutorial&#39;s subprocess resolves against it instead.</p><p>Put it under <code>environments/</code>, so <code>docs/environments/my-tutorial/</code> on disk. Give each tutorial that needs one its own, so two tutorials can hold incompatible versions without either having to bend. Each resolves standalone rather than joining the root <code>[workspace]</code>, which is also why the managed Dependabot config watches that path specifically. An environment elsewhere still builds, but nothing will ever update its dependencies.</p><p>That environment is yours, like <code>docs/Project.toml</code> is. The kit never writes it. Create the directory with a <code>Project.toml</code> declaring the tutorial&#39;s dependencies plus <code>Literate</code>, and a <code>[sources]</code> entry pointing at the package root so <code>using</code> your package resolves:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">deps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Literate = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;98b081ad-f1c9-55d3-8b20-4c87d4299306&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sources</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MyPackage = {path = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;../..&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The build instantiates it before the tutorial runs. A missing directory, a <code>Project.toml</code> without <code>Literate</code>, or one that will not resolve fails the build with a message naming the environment. It does not fall back to the shared environment: a page that quietly becomes its stub shows the reader a fast-build notice where the content should be.</p><p>Reach for this only when a dependency genuinely cannot co-resolve. Everything else belongs in <code>docs/Project.toml</code>, and a tutorial listed here pays for a second environment to resolve and precompile on every build.</p><h2 id="parallel-tutorials" tabindex="-1">Running heavy tutorials in parallel <a class="header-anchor" href="#parallel-tutorials" aria-label="Permalink to &quot;Running heavy tutorials in parallel {#parallel-tutorials}&quot;">​</a></h2><p>Heavy tutorials run one after another by default. Nothing orders them: each runs in its own subprocess against its own project, with no shared state. A package with several of them can therefore run them at once.</p><p><code>HEAVY_TUTORIAL_WORKERS</code> in <code>docs/docs_config.jl</code> sets how many execute concurrently. It defaults to <code>1</code>, which is the behaviour every package had before the setting existed.</p><p>The default stays serial because memory, not cores, is what bounds a sampling tutorial. Two concurrent Turing subprocesses can each hold several GB, and a runner that runs out of memory kills the build outright. Whether that is affordable depends on the package&#39;s own tutorials, so it is a per-package choice rather than an ecosystem-wide one.</p><p>The thread budget is divided, not multiplied. Each worker is launched with <code>JULIA_NUM_THREADS ÷ workers</code> threads, at least one, so two workers on a four-core runner get two threads each rather than four apiece. Total demand stays what a serial build already asked for. This trades parallelism within a tutorial (chains under <code>MCMCThreads()</code>) for parallelism between tutorials, so it wins when there are more tutorials than there are chains to spread across the cores.</p><p>Output stays attributable. Under a parallel build each tutorial&#39;s output is captured and printed as a single block when it finishes, headed by the tutorial&#39;s name, rather than interleaved with whatever else was sampling at the time. A failure does not cancel its siblings, and every failure is named at the end, so one broken tutorial cannot hide another.</p><p>Raise it when the docs build is running long against a CI time limit, and watch memory before going above <code>2</code> on a standard runner.</p><h2 id="What-stays-managed" tabindex="-1">What stays managed <a class="header-anchor" href="#What-stays-managed" aria-label="Permalink to &quot;What stays managed {#What-stays-managed}&quot;">​</a></h2><ul><li><p><code>docs/make.jl</code>, the thin caller into the kit&#39;s build logic.</p></li><li><p>The VitePress theme, config, and components under <code>docs/src/.vitepress/</code> and <code>docs/src/components/</code>.</p></li><li><p>The API reference pages, generated fresh from the module&#39;s docstrings on every build rather than stored as source.</p></li><li><p>The README standard sections (Contributing, How to cite, Code of conduct) between the <code>standard-sections</code> markers, so their wording stays consistent across adopters. Put package-specific prose outside the markers, and your citation details in <code>CITATION.cff</code>.</p></li></ul><p>Editing a managed file directly works until the next <code>update</code> or template-sync run reverts it — put customisation in the package-owned files above instead. If a package genuinely has to own a managed file, add an <code>EPIAWARE_MANAGED_OVERRIDE</code> comment to it and the sync preserves it; see <a href="/previews/PR473/getting-started/infrastructure#infrastructure">Infrastructure and template sync</a>.</p>`,31)])])}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
