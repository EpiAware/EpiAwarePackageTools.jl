import{_ as t,o as a,c as s,an as o}from"./chunks/framework.557DcZeb.js";const u=JSON.parse('{"title":"Test infrastructure","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/test-infrastructure.md","filePath":"getting-started/test-infrastructure.md","lastUpdated":null}'),i={name:"getting-started/test-infrastructure.md"};function r(n,e,c,d,l,p){return a(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="test-infrastructure" tabindex="-1">Test infrastructure <a class="header-anchor" href="#test-infrastructure" aria-label="Permalink to &quot;Test infrastructure {#test-infrastructure}&quot;">​</a></h1><p>The kit scaffolds a complete test setup so every adopting package runs the same checks the same way. This page describes what it writes, how the pieces fit together, and how to run and configure them. See <a href="/epiawarepackagetools.epiaware.org/previews/PR262/getting-started/infrastructure#infrastructure">Infrastructure and template sync</a> for the managed-versus-package-owned split that governs which of these files a sync rewrites.</p><h2 id="The-test-tree" tabindex="-1">The test tree <a class="header-anchor" href="#The-test-tree" aria-label="Permalink to &quot;The test tree {#The-test-tree}&quot;">​</a></h2><p><code>scaffold</code> lays down a <code>test/</code> tree with a clear split between the standard harness and your own tests.</p><ul><li><p><code>test/runtests.jl</code> (package-owned) is the entry point. It discovers <code>@testitem</code>s with TestItemRunner and runs them.</p></li><li><p><code>test/package/quality.jl</code> (managed) holds the standard quality testset.</p></li><li><p><code>test/package/qa_config.jl</code> (package-owned) supplies the package-specific inputs the quality testset needs.</p></li><li><p><code>test/jet/</code> and <code>test/formatter/</code> (managed runners, package-owned config) are isolated environments for the JET and JuliaFormatter checks.</p></li><li><p><code>test/Project.toml</code> (package-owned) is the test environment, seeded with the dependencies the shared helpers need.</p></li></ul><p>Your package&#39;s own unit tests live alongside these as further <code>@testitem</code>s anywhere under <code>test/</code>.</p><h2 id="TestItemRunner-and-@testitem" tabindex="-1">TestItemRunner and <code>@testitem</code> <a class="header-anchor" href="#TestItemRunner-and-@testitem" aria-label="Permalink to &quot;TestItemRunner and `@testitem` {#TestItemRunner-and-@testitem}&quot;">​</a></h2><p>Tests are written as <code>@testitem</code> blocks rather than a nested tree of <code>include</code>d files. Each item is an isolated unit that TestItemRunner discovers by walking the test directory, so there is no central file listing every test to keep up to date. Items carry tags, and the runner selects which to run by filtering on those tags.</p><p><code>test/runtests.jl</code> uses this to expose a few run modes through test arguments.</p><ul><li><p>No argument runs every item except the AD-tagged ones (see <a href="/epiawarepackagetools.epiaware.org/previews/PR262/getting-started/ad-tooling#ad-tooling">AD tooling</a>).</p></li><li><p><code>skip_quality</code> skips the <code>:quality</code>-tagged items for fast local iteration.</p></li><li><p><code>quality_only</code> runs only the quality testset.</p></li><li><p><code>readme_only</code> runs only <code>:readme</code>-tagged items.</p></li></ul><p>Discovery is restricted to the package&#39;s own test tree, so a nested worktree or a sibling directory sharing a path prefix is never globbed in.</p><h2 id="The-quality-testset" tabindex="-1">The quality testset <a class="header-anchor" href="#The-quality-testset" aria-label="Permalink to &quot;The quality testset {#The-quality-testset}&quot;">​</a></h2><p><code>test/package/quality.jl</code> routes every generic quality check through the shared <code>EpiAwarePackageTools</code> helpers, so a package gets the whole set without reimplementing any of it. Each check is a <code>:quality</code>-tagged <code>@testitem</code>.</p><ul><li><p>Aqua (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_aqua"><code>test_aqua</code></a>) for method ambiguities, unbound type parameters, stale dependencies, and other common package defects.</p></li><li><p>ExplicitImports (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_explicit_imports"><code>test_explicit_imports</code></a>) so every used name is explicitly imported rather than pulled in implicitly.</p></li><li><p>Import centralisation (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_import_centralisation"><code>test_import_centralisation</code></a>) so <code>using</code> and <code>import</code> statements sit in the module file rather than scattered across sources.</p></li><li><p>Docstring format (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_docstring_format"><code>test_docstring_format</code></a>) for the standard docstring conventions.</p></li><li><p>README sections (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_readme_sections"><code>test_readme_sections</code></a>) so the standard README structure stays intact.</p></li><li><p>Doctests (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_doctest"><code>test_doctest</code></a>) so the examples in docstrings still run.</p></li><li><p>Formatting (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_formatting"><code>test_formatting</code></a>) so the source matches the pinned formatter.</p></li><li><p>Linting with JET (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_linting"><code>test_linting</code></a>) for static analysis.</p></li><li><p>Extension ambiguities (<a href="/epiawarepackagetools.epiaware.org/previews/PR262/lib/public#EpiAwarePackageTools.test_ext_ambiguities"><code>test_ext_ambiguities</code></a>) for packages that ship extensions.</p></li></ul><p>The check logic is managed and stays in <code>quality.jl</code>. Everything a check needs that is specific to your package lives in the package-owned <code>qa_config.jl</code> as a <code>QA_CONFIG</code> named tuple, so you tune the checks without editing the managed file. <code>QA_CONFIG</code> carries the module under test, the JET environment path, per-check Aqua relaxations, ExplicitImports ignore lists, docstring cross-reference ignores, the README requirements, and the list of extensions to ambiguity-check.</p><h2 id="Isolated-JET-and-formatter-environments" tabindex="-1">Isolated JET and formatter environments <a class="header-anchor" href="#Isolated-JET-and-formatter-environments" aria-label="Permalink to &quot;Isolated JET and formatter environments {#Isolated-JET-and-formatter-environments}&quot;">​</a></h2><p>JET and JuliaFormatter each pin their own version of JuliaSyntax, and those pins clash with each other and with the main test dependencies. The kit therefore runs each in its own environment under <code>test/jet/</code> and <code>test/formatter/</code>, invoked as a subprocess by the quality testset.</p><p>The formatter check reports any file under <code>src</code>, <code>test</code>, <code>docs</code>, or <code>benchmark</code> that is not formatted, without modifying it. The JET runner fails on any static-analysis report by default. A package whose public surface is DynamicPPL <code>@model</code> functions can drop a package-owned <code>test/jet/jet_config.jl</code> defining a <code>JET_REPORT_FILTER</code> predicate to suppress the spurious reports the tilde macro produces; <code>dynamicppl_model_filter</code> is the ready-made filter for that case.</p><h2 id="Running-the-tests" tabindex="-1">Running the tests <a class="header-anchor" href="#Running-the-tests" aria-label="Permalink to &quot;Running the tests {#Running-the-tests}&quot;">​</a></h2><p>From a Julia session, <code>Pkg.test()</code> runs the full suite. The scaffolded <code>Taskfile.yml</code> wraps the common modes.</p><ul><li><p><code>task test</code> runs the full suite including quality and AD gradient tests.</p></li><li><p><code>task test-fast</code> skips the quality checks for development.</p></li><li><p><code>task test-quality</code> runs only the quality checks.</p></li><li><p><code>task test-jet</code> and <code>task test-formatting</code> run those checks in their isolated environments.</p></li></ul><p>The formatter gate also runs in CI through the managed <code>pre-commit.yaml</code> workflow, pinned to the same formatter version as the local pre-commit hook and the isolated formatter environment so a local format and CI never disagree.</p>',22)])])}const g=t(i,[["render",r]]);export{u as __pageData,g as default};
