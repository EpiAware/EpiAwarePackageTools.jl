import{_ as o,o as t,c as a,an as s}from"./chunks/framework.D719Nim3.js";const u=JSON.parse('{"title":"Release notes","description":"","frontmatter":{},"headers":[],"relativePath":"release-notes.md","filePath":"release-notes.md","lastUpdated":null}'),d={name:"release-notes.md"};function n(c,e,r,i,l,h){return t(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="Release-notes" tabindex="-1">Release notes <a class="header-anchor" href="#Release-notes" aria-label="Permalink to &quot;Release notes {#Release-notes}&quot;">​</a></h1><p>EpiAwarePackageTools.jl follows a hybrid changelog approach:</p><ul><li><p><strong>GitHub Releases</strong>: All releases (automatically generated by TagBot)</p></li><li><p><strong>NEWS.md</strong>: Major releases requiring detailed context (shown below)</p></li></ul><p>See <a href="https://github.com/EpiAware/EpiAwarePackageTools.jl/releases" target="_blank" rel="noreferrer">GitHub Releases</a> for minor/patch release information.</p><h2 id="Unreleased" tabindex="-1">Unreleased <a class="header-anchor" href="#Unreleased" aria-label="Permalink to &quot;Unreleased {#Unreleased}&quot;">​</a></h2><p><strong>New</strong>: the managed <code>.github/dependabot.yml</code> now watches the isolated test environments (<code>test/jet</code>, <code>test/formatter</code>, and with <code>ad = true</code> also <code>test/ad</code> and <code>test/ADFixtures</code>) via a second <code>julia</code> entry over <code>/test/*</code>. Those directories are deliberately not <code>[workspace]</code> members — that is what keeps a heavy QA dependency&#39;s own pins from clashing with the shared test deps — so the existing <code>directory: &quot;/&quot;</code> entry had never seen them, and their compat entries drifted away from <code>test/Project.toml</code>&#39;s. The drift is quiet and it is the bad kind: a bump to <code>test/Project.toml</code> goes green while the environment that actually runs the check stays on the old version, so the green says nothing about the new one. #337 is exactly that shape — it widens <code>test/Project.toml</code> to admit JET 0.12 while <code>test/jet/Project.toml</code>, where JET actually runs, stays pinned to <code>0.11</code>. The isolated bumps land in their own grouped PR (<code>julia-isolated</code>) rather than joining the workspace group, because a JET or JuliaFormatter minor is a breaking bump under 0.x semver and wants reviewing on its own.</p><p>The managed <code>test/jet/Project.toml</code> pin moves from <code>JET = &quot;0.11&quot;</code> to <code>JET = &quot;0.11, 0.12&quot;</code>, so the isolated runner resolves the version <code>test/Project.toml</code> already admits. JET 0.12&#39;s breaking removals do not reach this kit: <code>test_jet</code> and the isolated runner both pass a module to <code>report_package</code>/<code>test_package</code> with explicit <code>target_modules</code>, and neither uses <code>.JET.toml</code>, <code>target_defined_modules</code>, <code>fullpath</code>, nor compiler keywords such as <code>max_methods</code>. The behavioural change to watch is that 0.12 stops treating <code>Base</code> as a submodule of <code>Main</code> for <code>target_modules</code> matching, which does not affect a <code>target_modules = (YourPackage,)</code> call.</p><p>An adopter gets both on their next sync; neither needs a per-repo edit.</p><p>The standard README structure now requires a <code>## Related packages</code> section, in the slot after Getting started and before the Documentation section (#292). <code>test_readme_sections</code> also reports the retired <code>What packages work well with X?</code> heading as drift, naming <code>## Related packages</code> as its replacement, so the fix is a rename rather than a hunt for what is missing. The scaffolded README skeleton seeds the heading and a placeholder bullet; the bullets themselves are package-specific (one sentence per sibling with a real relationship, linked to that sibling&#39;s docs) and cannot be templated. Four adopting packages do not carry the section in that slot yet, so bumping to this version reds their README check until each is updated.</p><p>Three further README checks ship alongside, none of them wired into the scaffolded quality testset: turning them on for adopting packages is a separate rollout decision. <code>test_readme_placeholders</code> fails when a README still carries the scaffold&#39;s unfilled placeholder text, deriving its patterns from the seeded skeleton so it tracks the template. <code>test_readme_prose</code> fails on a banned-word list and on sentences over a configurable length, reading only prose (code fences, tables, link URLs, and inline code are skipped). <code>test_readme_bullets</code> checks the Why section&#39;s bullets: it flags the feature-inventory form (<code>**Label**: ...</code>) that #292 rules out in favour of motivation sentences, bounds the bullet count, and flags a multi-sentence bullet.</p><p>A package that declares <code>[extensions]</code> now gets an &quot;Extensions&quot; group in its docs nav, one entry per extension, each pointing at a seeded page under <code>docs/src/extensions/</code> (#319). The extensions are read from the package&#39;s own <code>Project.toml</code> rather than gated by a kwarg: an extension is a fact about the package, unlike the benchmark and AD opt-ins. The pages are package-owned and write-once, so authored scope prose survives every sync, and each ships its public-API <code>@autodocs</code> block inert, as a code sample inside an outer fence — an extension module exists only once its weakdeps load, so a live block would red the docs build of a package whose docs environment does not carry them yet. An HTML comment would not do: Documenter parses with the <code>Markdown</code> stdlib, which has no CommonMark HTML-block handling, so a fence inside <code>&lt;!-- --&gt;</code> is still live to it (the quirk behind #301/#304). A page that the write-once nav in <code>docs/pages.jl</code> does not list is reported as a warning naming the entry to add, rather than left unreachable. A build drops any Extensions entry whose page is missing, so a package that was scaffolded before this, or that removed an extension, never publishes a dangling link. <code>docs/pages.jl</code> is package-owned and written once, so an already-scaffolded package adds the group by hand, as it does today when it flips <code>benchmarks = true</code>. A new managed workflow, <code>.github/workflows/release-nudge.yaml</code>, is scaffolded into every adopting package (thin caller of a new EpiAware/.github reusable, <code>release-nudge.yml</code>). It runs weekly and on demand, compares <code>Project.toml</code>&#39;s version and the commits on <code>main</code> against the latest release/tag (and, best-effort, the version registered in the Julia General registry), and opens or refreshes a single labelled issue telling a maintainer what to do — bump via <code>/version</code>, update <code>NEWS.md</code>, then <code>/register</code> — whenever there are unreleased changes, closing it once everything is released. The issue is never edited in place: a stale one (state changed, or simply sat open too long) is closed with a &quot;superseded&quot; comment and replaced. The generated issue body can never contain a literal <code>@</code>, so neither a contributor&#39;s handle nor the registry bot&#39;s own handle can ever render as an accidental mention or trigger.</p><p>The managed <code>.github/dependabot.yml</code> now runs both ecosystems daily rather than weekly (#312). Both were already grouped by a wildcard pattern (#249), so each run refreshes the one open grouped PR per ecosystem instead of opening more: a shorter interval buys faster reusable-workflow and dependency updates at the same open PR count. The cost is CI, not review load: each refresh is a new commit and so a new check run, where a weekly interval let a week&#39;s bumps share one. A package that would rather trade update latency for runner time can set the <code>julia</code> ecosystem — the expensive half, since a refresh reruns the full test matrix — back to <code>weekly</code> in its own copy; the grouping is what matters. Adopters pick this up on their next <code>update</code>.</p><p><strong>Breaking</strong>: <code>scaffold_update</code> is renamed back to <code>update</code>, and is now <code>public</code>, not <code>export</code>ed (#294). A bare <code>using EpiAwarePackageTools</code> no longer brings it into scope — call it as <code>EpiAwarePackageTools.update(...)</code> or <code>using EpiAwarePackageTools: update</code>.</p><p>This function used to be a bare <code>update</code> until #173 found that an <code>export</code>ed generic verb collides with a package&#39;s own same-named export: <code>using EpiAwarePackageTools; using ComposedDistributions</code> left <code>update</code> unbound in <code>Main</code>, breaking Documenter <code>@ref</code> resolution across every kit-adopting package that also ships its own <code>update</code>. #178 fixed it at the time with a hard rename to <code>scaffold_update</code>. <code>public</code> closes that collision a different way: a <code>public</code>-not-<code>export</code>ed name is never brought into scope by a bare <code>using</code>, so it cannot fight another package&#39;s export regardless of what either package calls its own verb — that makes the short, generic name safe again, so #294 renames it back.</p><p><code>scaffold_update</code> is kept <code>public</code> too, as a transitional alias (<code>const scaffold_update = update</code>) — an already-qualified caller (<code>EpiAwarePackageTools.scaffold_update(...)</code>, or an explicit <code>using EpiAwarePackageTools: scaffold_update</code>) keeps working unchanged. The alias is removed in a future cleanup once adopters have moved onto <code>update</code>.</p><p>Every template-sync/self-drift caller shipped by the kit now calls <code>update</code>; an adopter on an older scaffolded <code>.github/workflows/template-sync.yaml</code> (calling <code>scaffold_update(...)</code> unqualified after a bare <code>using EpiAwarePackageTools</code>) needs a one-line fix — qualify the call — before their first sync on this kit version, or the sync run itself fails.</p><p><code>build_index</code>&#39;s HTML-comment strip now recognises the two remaining CommonMark code forms it previously missed: a 4-space indented code block and an inline single-backtick code span. A <code>&lt;!-- --&gt;</code> shown as literal example text inside either now survives verbatim on the generated docs index, closing the gap #301/PR#304 deliberately left open (#306).</p><p><code>build_docs</code> no longer flags a package&#39;s own module docstring as &quot;not included in the manual&quot; under <code>checkdocs = :all</code> (the default for a package with no re-exports): it is now rendered as its own <code>@docs</code> block prepended to <code>lib/public.md</code>, ahead of Contents/Index, so it both satisfies the completeness scan and is actually readable on the built site (#313).</p><p>The managed <code>Taskfile.yml</code> <code>coverage</code> task now instruments the actual test run. It previously passed <code>--code-coverage=user</code> to the outer <code>julia</code> process while <code>Pkg.test()</code> spawns its own child for the real run — the child was never instrumented, so every <code>task coverage</code> produced an <code>lcov.info</code> with no real per-file numbers. The recipe now passes <code>Pkg.test(coverage=true, ...)</code> directly, and the post-processing step&#39;s <code>Pkg.add(&quot;Coverage&quot;)</code> moved to a shared <code>@coverage</code> environment so it no longer dirties adopters&#39; tracked <code>test/Project.toml</code> (#315).</p><p>The managed <code>Quality: formatting</code> testitem now runs through the package&#39;s isolated, exactly-pinned <code>test/formatter/</code> environment (the same isolation <code>test_linting</code> already used for JET), instead of resolving JuliaFormatter from the shared test environment where its version floats with the CI Julia in the matrix. Packages get this automatically once <code>qa_config.jl</code> gains a <code>formatter_env</code> key on their next <code>scaffold</code>; adopters whose package-owned <code>qa_config.jl</code> predates the key keep today&#39;s in-process check via a <code>hasproperty</code> guard that warns when it engages, matching the existing <code>QA_CONFIG.readme</code> fallback idiom (#188, #321).</p><p><code>update</code> now seeds a missing <code>CITATION.cff</code> (write-once, like <code>scaffold</code>) instead of only ever rendering the managed &quot;How to cite&quot; README section that points at it. A package that adopted the template before citation seeding existed used to carry a permanently dangling <code>CITATION.cff</code> link that no <code>update</code> could ever fix — a hard docs-linkcheck 404 the moment its README reached a linkchecked build (#322).</p><p><code>update</code>/<code>scaffold_update</code> now warns before a resync silently reverts a caller job that was repointed at a repo-local reusable workflow to add its own <code>with:</code> inputs. <code>_CALLER_JOB</code> only keys the org&#39;s shared-reusable <code>uses:</code> shape, so a job pointed at <code>./.github/workflows/&lt;file&gt;.yaml</code> instead was never seen and never preserved: the sync would quietly drop its inputs and revert it to the shared reusable, exactly what happened to <code>DistributionsInference.jl</code>&#39;s <code>downgrade-compat</code> job. The job still reverts — that behaviour is unchanged — but the loss now shows up as a <code>@warn</code> and an entry in the returned <code>warnings</code> vector, so it surfaces in the sync PR instead of as an unrelated-looking red check days later (#325).</p>',22)])])}const g=o(d,[["render",n]]);export{u as __pageData,g as default};
