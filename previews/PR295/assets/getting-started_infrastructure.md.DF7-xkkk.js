import{_ as a,o as t,c as i,an as s}from"./chunks/framework.B_QK-hAl.js";const g=JSON.parse('{"title":"Infrastructure and template sync","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/infrastructure.md","filePath":"getting-started/infrastructure.md","lastUpdated":null}'),n={name:"getting-started/infrastructure.md"};function o(r,e,d,c,h,l){return t(),i("div",null,[...e[0]||(e[0]=[s('<h1 id="infrastructure" tabindex="-1">Infrastructure and template sync <a class="header-anchor" href="#infrastructure" aria-label="Permalink to &quot;Infrastructure and template sync {#infrastructure}&quot;">​</a></h1><p>The kit does two jobs for an adopting package: it writes the standard infrastructure once (<code>scaffold</code>), and it keeps that infrastructure current afterwards (<code>update</code>, driven on a schedule). This page explains the sync machinery and how the kit applies it to itself on its own repository. For what that infrastructure actually contains, see the reference pages on the <a href="/previews/PR295/getting-started/test-infrastructure#test-infrastructure">test infrastructure</a>, <a href="/previews/PR295/getting-started/benchmarking#benchmarking">benchmarking</a>, and <a href="/previews/PR295/getting-started/ad-tooling#ad-tooling">AD tooling</a>.</p><h2 id="Managed-and-package-owned-files" tabindex="-1">Managed and package-owned files <a class="header-anchor" href="#Managed-and-package-owned-files" aria-label="Permalink to &quot;Managed and package-owned files {#Managed-and-package-owned-files}&quot;">​</a></h2><p>Every file the kit writes is one of two kinds.</p><ul><li><p>Managed files are the standard infrastructure: the CI caller workflows, the documentation build (<code>docs/make.jl</code> and the VitePress theme, config, and components), the formatter and pre-commit config, and the coverage config. <code>update</code> rewrites them from the bundled templates on every sync, so drift is removed automatically. Each managed file carries a <code>MANAGED by EpiAwarePackageTools.scaffold</code> header; do not edit them by hand.</p></li><li><p>Package-owned files are written once and never overwritten: the package&#39;s unit tests, its QA config values, the navigation tree (<code>docs/pages.jl</code>), the README body, <code>LICENSE</code>, <code>CITATION.cff</code> (your citation metadata), <code>NEWS.md</code> (see the <a href="/previews/PR295/getting-started/changelog#changelog">changelog convention</a> for its shape), and the docs source pages such as this one. These are yours to edit.</p></li></ul><p>The README badge block, the README standard sections (Contributing, How to cite, Code of conduct), and the <code>.gitignore</code> standard rules are a hybrid: they are managed between markers, so their wording and the ignore rules stay current while anything you add outside the markers is preserved. The managed &quot;How to cite&quot; section points at the package-owned <code>CITATION.cff</code>, so GitHub renders a &quot;Cite this repository&quot; widget and the citation content stays yours to edit.</p><h2 id="Overriding-a-managed-file" tabindex="-1">Overriding a managed file <a class="header-anchor" href="#Overriding-a-managed-file" aria-label="Permalink to &quot;Overriding a managed file {#Overriding-a-managed-file}&quot;">​</a></h2><p>A package that must keep its own version of a managed file says so in the file. Put the marker <code>EPIAWARE_MANAGED_OVERRIDE</code> in a comment anywhere in it, and <code>update</code> preserves the file rather than resyncing it, reporting it under <code>preserved</code> in the manifest.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># EPIAWARE_MANAGED_OVERRIDE: this package needs its own test matrix.</span></span></code></pre></div><p>Remove the marker to hand the file back to the kit. <code>scaffold</code> with <code>force = true</code> ignores the marker and lays the managed file down fresh, so a new package always starts fully managed.</p><p>Use this sparingly. An overridden file stops tracking the standard, so kit fixes no longer reach it, which is the opposite of what the kit is for. Prefer the supported hooks (the package-owned config values, the marker-delimited regions, and the <code>ad</code>/<code>benchmarks</code>/<code>downgrade_compat</code> flags) where they cover the need.</p><p>What the marker does <strong>not</strong> cover:</p><ul><li><p>The marker-delimited regions described above, in files that are otherwise package-owned: the README badge block, the README standard sections, the <code>.gitignore</code> managed block, and the <code>[workspace]</code> stanza in <code>Project.toml</code>. Those are refreshed on every sync whether or not the file carries the marker. Customise them by editing outside their markers, which is what the markers are for. There is no region-level opt-out.</p></li><li><p>Retirement. When the kit retires a path, a sync deletes it whether or not it carries the marker, because a retired path is infrastructure the kit no longer supports at all.</p></li><li><p>The two managed JSON files (<code>docs/package.json</code> and <code>.secrets.baseline</code>). The marker has to live in a comment and JSON has no comments, so these cannot be overridden this way.</p></li></ul><p>The match is case-sensitive: write <code>EPIAWARE_MANAGED_OVERRIDE</code> in capitals. A mis-cased marker does nothing and the file is resynced as usual.</p><p>The AD-harness driver <code>test/ad/setup.jl</code> is the original case: a package whose <code>ADFixtures</code> registry predates the current <code>ADRegistry</code> contract must keep a hand-written driver while it migrates. That file also honours its older marker <code>EPIAWARE_AD_SETUP_OWNED</code>; either marker preserves it. It is also the one file where <code>update</code> warns before overwriting a version that has diverged from the kit&#39;s and carries no marker, because a clobber there breaks every AD CI job. No such warning exists for managed files generally: divergence from the current template is the normal state of an adopter that is simply on an older kit version, so a general divergence warning would fire on every sync and tell you nothing. Mark the files you own instead.</p><p>A bespoke <code>docs/make.jl</code> is the other case the marker is meant for. The managed <code>make.jl</code> is a thin <a href="/previews/PR295/lib/public#EpiAwarePackageTools.DocsBuild.build_docs"><code>build_docs</code></a> entry point, which regenerates <code>docs/src/index.md</code> from the README and reads a package-owned <code>docs/docs_config.jl</code>. A package that has deliberately kept a direct <code>DocumenterVitepress</code> build with a hand-authored home page marks its <code>make.jl</code>, and <code>update</code> then leaves the build alone instead of migrating it on a routine sync.</p><h2 id="What-the-kit-preserves-without-a-marker" tabindex="-1">What the kit preserves without a marker <a class="header-anchor" href="#What-the-kit-preserves-without-a-marker" aria-label="Permalink to &quot;What the kit preserves without a marker {#What-the-kit-preserves-without-a-marker}&quot;">​</a></h2><p>Some parts of a managed file are adopter configuration rather than standard, and the kit recovers them from the committed repository on every sync, so the file keeps tracking the standard while the customisation survives. No marker is needed, and none of these values has to be re-passed to <code>update</code>.</p><ul><li><p>The reviewer handle, the docs-hosting choice, the benchmark and downgrade-compat opt-ins, Dependabot&#39;s action and reusable-workflow pins, and any package-owned <code>with:</code> input added to a managed CI caller.</p></li><li><p>The Zenodo DOI badge and the licence badge in the README. A non-MIT package keeps its licence badge across a sync; pass <code>license</code> explicitly only to change it.</p></li><li><p>The <code>downstreams</code> list in <code>.github/workflows/downstream.yaml</code>. Which packages depend on yours is a fact about your package, so the list you commit wins over the template&#39;s empty seed, while the rest of the workflow stays managed.</p></li></ul><h2 id="Staying-in-sync" tabindex="-1">Staying in sync <a class="header-anchor" href="#Staying-in-sync" aria-label="Permalink to &quot;Staying in sync {#Staying-in-sync}&quot;">​</a></h2><p>Two workflows keep an adopting package aligned with the kit.</p><ul><li><p>The scheduled template-sync workflow (<code>.github/workflows/template-sync.yaml</code>) re-runs <code>update</code> against the repository on a schedule and on Dependabot updates, then opens or refreshes a pull request whenever the committed infrastructure has drifted from the current standard.</p></li><li><p>Dependabot (<code>.github/dependabot.yml</code>) keeps the pinned reusable-workflow and action references current, so fixes in the shared workflows reach the repository without manual edits.</p></li></ul><p>An improvement made once in the kit therefore propagates to every adopting package on the next sync.</p><h2 id="Registration-safety" tabindex="-1">Registration safety <a class="header-anchor" href="#Registration-safety" aria-label="Permalink to &quot;Registration safety {#Registration-safety}&quot;">​</a></h2><p>The managed <code>registrability.yaml</code> caller runs the shared <code>EpiAware/.github</code> registrability workflow whenever a package&#39;s <code>Project.toml</code> changes (and on demand, and on <code>main</code>). It runs two read-only checks.</p><ul><li><p>Registrability asserts that every non-stdlib <code>[deps]</code> entry exists in the General registry with a version satisfying the package&#39;s own <code>[compat]</code>. A dependency pinned in <code>[sources]</code> to a git revision is unregistered, so the check fails with a per-dependency message. This is the failure that had ConvolvedDistributions 0.2.0 rejected from General with nothing in CI to catch it.</p></li><li><p>The reverse-dependency scan reports which org packages depend on this one and whether their <code>[compat]</code> admits the version under test. A breaking release legitimately strands a downstream bound, so this is a warning by default; set <code>fail_on_revdep_break: true</code> on the caller job to gate on it.</p></li></ul><h2 id="How-the-kit-applies-this-to-itself" tabindex="-1">How the kit applies this to itself <a class="header-anchor" href="#How-the-kit-applies-this-to-itself" aria-label="Permalink to &quot;How the kit applies this to itself {#How-the-kit-applies-this-to-itself}&quot;">​</a></h2><p>The kit manages its own repository the same way an adopter&#39;s is managed, with one difference: it is a tooling package, so it scaffolds itself with <code>ad = false</code> (no AD CI or harness).</p><p>A <code>self-drift</code> CI check runs <code>update(&quot;.&quot;; ad = false)</code> and asserts the result is zero drift, proving the committed infrastructure matches what the templates currently produce. Because the kit is its own first adopter, this documentation site and its generated pages are the live example described in the <a href="/previews/PR295/getting-started/index#getting-started">getting-started note</a>: what you see here is exactly what the scaffold writes.</p><h2 id="Running-a-sync-by-hand" tabindex="-1">Running a sync by hand <a class="header-anchor" href="#Running-a-sync-by-hand" aria-label="Permalink to &quot;Running a sync by hand {#Running-a-sync-by-hand}&quot;">​</a></h2><p>You can drive the same sync from a Julia session:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwarePackageTools</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Re-apply the managed standard files and report drift. `update` is</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># `public`, not exported (#294), so call it qualified.</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EpiAwarePackageTools</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pkgdir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MyPackage))</span></span></code></pre></div><p><code>update</code> rewrites only the managed files and returns a manifest of what was created, updated, or preserved; package-owned files are left untouched.</p>',33)])])}const u=a(n,[["render",o]]);export{g as __pageData,u as default};
