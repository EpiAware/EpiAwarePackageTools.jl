import{_ as t,o as a,c as s,an as i}from"./chunks/framework.DPMldsPj.js";const g=JSON.parse('{"title":"Infrastructure and template sync","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/infrastructure.md","filePath":"getting-started/infrastructure.md","lastUpdated":null}'),o={name:"getting-started/infrastructure.md"};function n(r,e,d,h,c,l){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="infrastructure" tabindex="-1">Infrastructure and template sync <a class="header-anchor" href="#infrastructure" aria-label="Permalink to &quot;Infrastructure and template sync {#infrastructure}&quot;">​</a></h1><p>The kit does two jobs for an adopting package: it writes the standard infrastructure once (<code>scaffold</code>), and it keeps that infrastructure current afterwards (<code>update</code>, driven on a schedule). This page explains the sync machinery and how the kit applies it to itself on its own repository. For what that infrastructure actually contains, see the reference pages on the <a href="/previews/PR484/getting-started/test-infrastructure#test-infrastructure">test infrastructure</a>, <a href="/previews/PR484/getting-started/benchmarking#benchmarking">benchmarking</a>, and <a href="/previews/PR484/getting-started/ad-tooling#ad-tooling">AD tooling</a>.</p><h2 id="Managed-and-package-owned-files" tabindex="-1">Managed and package-owned files <a class="header-anchor" href="#Managed-and-package-owned-files" aria-label="Permalink to &quot;Managed and package-owned files {#Managed-and-package-owned-files}&quot;">​</a></h2><p>Every file the kit writes is one of two kinds.</p><ul><li><p>Managed files are the standard infrastructure: the CI caller workflows, the documentation build (<code>docs/make.jl</code> and the VitePress theme, config, and components), the formatter and pre-commit config, and the coverage config. <code>update</code> rewrites them from the bundled templates on every sync, so drift is removed automatically. Each managed file carries a <code>MANAGED by EpiAwarePackageTools.scaffold</code> header; do not edit them by hand.</p></li><li><p>Package-owned files are written once and never overwritten: the package&#39;s unit tests, its QA config values, the navigation tree (<code>docs/pages.jl</code>), the README body, <code>LICENSE</code>, <code>CITATION.cff</code> (your citation metadata), and the docs source pages such as this one. These are yours to edit.</p></li></ul><p>The README badge block, the README standard sections (Contributing, How to cite, Code of conduct), the <code>.gitignore</code> standard rules, and the docs pointers in <code>AGENTS.md</code> and <code>CLAUDE.md</code> are a hybrid: they are managed between markers, so their wording and the ignore rules stay current while anything you add outside the markers is preserved. The managed &quot;How to cite&quot; section points at the package-owned <code>CITATION.cff</code>, so GitHub renders a &quot;Cite this repository&quot; widget and the citation content stays yours to edit.</p><p><code>Project.toml</code> is package-owned like the rest, but a sync also warns (never edits) if it carries a comment: Pkg silently drops any comment the next time something rewrites the file (<code>Pkg.add</code>, <code>Pkg.compat!</code>, a dependency bump), so one left there reads fine today and vanishes without a trace on the first routine edit.</p><h2 id="Agent-files" tabindex="-1">Agent files <a class="header-anchor" href="#Agent-files" aria-label="Permalink to &quot;Agent files {#Agent-files}&quot;">​</a></h2><p><code>AGENTS.md</code> links the docs an agent should read: <a href="/previews/PR484/standards#standards">Package standards</a>, <a href="/previews/PR484/getting-started/test-infrastructure#test-infrastructure">Test infrastructure</a>, this page, and the package&#39;s own documentation. <code>CLAUDE.md</code> points at <code>AGENTS.md</code>. Neither restates a standard, because a copy drifts from the page it was copied from.</p><p>Both carry the managed block between <code>&lt;!-- epiaware-standards:start MANAGED by EpiAwarePackageTools.scaffold --&gt;</code> and <code>&lt;!-- epiaware-standards:end --&gt;</code>. Edit the block in the kit&#39;s <code>templates/AGENTS.md</code> and <code>templates/CLAUDE.md</code>, not in the adopting package. A package&#39;s own notes go after the end marker and survive every sync.</p><p>Both files are read into an agent&#39;s context in full at the start of every session, so the block spends one word on saying it is managed and leaves the rest to this page. The marker is matched on its prefix, so a package carrying an older, longer form is rewritten to this one on the next sync.</p><h2 id="Overriding-a-managed-file" tabindex="-1">Overriding a managed file <a class="header-anchor" href="#Overriding-a-managed-file" aria-label="Permalink to &quot;Overriding a managed file {#Overriding-a-managed-file}&quot;">​</a></h2><p>A package that must keep its own version of a managed file says so in the file. Put the marker <code>EPIAWARE_MANAGED_OVERRIDE</code> in a comment anywhere in it, and <code>update</code> preserves the file rather than resyncing it, reporting it under <code>preserved</code> in the manifest.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># EPIAWARE_MANAGED_OVERRIDE: this package needs its own test matrix.</span></span></code></pre></div><p>Remove the marker to hand the file back to the kit. <code>scaffold</code> with <code>force = true</code> ignores the marker and lays the managed file down fresh, so a new package always starts fully managed.</p><p>Use this sparingly. An overridden file stops tracking the standard, so kit fixes no longer reach it, which is the opposite of what the kit is for. Prefer the supported hooks (the package-owned config values, the marker-delimited regions, and the <code>ad</code>/<code>benchmarks</code>/<code>downgrade_compat</code>/<code>unregistered_sources</code> flags) where they cover the need.</p><p>What the marker does <strong>not</strong> cover:</p><ul><li><p>The marker-delimited regions described above, in files that are otherwise package-owned: the README badge block, the README standard sections, the <code>.gitignore</code> managed block, the <code>CLAUDE.md</code> standards block, and the <code>[workspace]</code> stanza in <code>Project.toml</code>. Those are refreshed on every sync whether or not the file carries the marker. Customise them by editing outside their markers, which is what the markers are for. There is no region-level opt-out.</p></li><li><p>Retirement. When the kit retires a path, a sync deletes it whether or not it carries the marker, because a retired path is infrastructure the kit no longer supports at all.</p></li><li><p>The two managed JSON files (<code>docs/package.json</code> and <code>.secrets.baseline</code>). The marker has to live in a comment and JSON has no comments, so these cannot be overridden this way.</p></li></ul><p>The match is case-sensitive: write <code>EPIAWARE_MANAGED_OVERRIDE</code> in capitals. A mis-cased marker does nothing and the file is resynced as usual.</p><p>The AD-harness driver <code>test/ad/setup.jl</code> is the original case: a package whose <code>ADFixtures</code> registry predates the current <code>ADRegistry</code> contract must keep a hand-written driver while it migrates. That file also honours its older marker <code>EPIAWARE_AD_SETUP_OWNED</code>; either marker preserves it. It is also the one file where <code>update</code> warns before overwriting a version that has diverged from the kit&#39;s and carries no marker, because a clobber there breaks every AD CI job. No such warning exists for managed files generally: divergence from the current template is the normal state of an adopter that is simply on an older kit version, so a general divergence warning would fire on every sync and tell you nothing. Mark the files you own instead.</p><p>A bespoke <code>docs/make.jl</code> is the other case the marker is meant for. The managed <code>make.jl</code> is a thin <a href="/previews/PR484/lib/public#EpiAwarePackageTools.DocsBuild.build_docs"><code>build_docs</code></a> entry point, which regenerates <code>docs/src/index.md</code> from the README and reads a package-owned <code>docs/docs_config.jl</code>. A package that has deliberately kept a direct <code>DocumenterVitepress</code> build with a hand-authored home page marks its <code>make.jl</code>, and <code>update</code> then leaves the build alone instead of migrating it on a routine sync.</p><h2 id="What-the-kit-preserves-without-a-marker" tabindex="-1">What the kit preserves without a marker <a class="header-anchor" href="#What-the-kit-preserves-without-a-marker" aria-label="Permalink to &quot;What the kit preserves without a marker {#What-the-kit-preserves-without-a-marker}&quot;">​</a></h2><p>Some parts of a managed file are adopter configuration rather than standard, and the kit recovers them from the committed repository on every sync, so the file keeps tracking the standard while the customisation survives. No marker is needed, and none of these values has to be re-passed to <code>update</code>.</p><ul><li><p>The reviewer handle, the docs-hosting choice, the benchmark and downgrade-compat opt-ins, Dependabot&#39;s action and reusable-workflow pins, and any package-owned <code>with:</code> input added to a managed CI caller.</p></li><li><p>The Zenodo DOI badge and the licence badge in the README. A non-MIT package keeps its licence badge across a sync; pass <code>license</code> explicitly only to change it. A licence outside the supported set fails the sync instead of being relabelled to the default.</p></li><li><p>The <code>downstreams</code> list in <code>.github/workflows/downstream.yaml</code>. Which packages depend on yours is a fact about your package, so the list you commit wins over the template&#39;s empty seed, while the rest of the workflow stays managed.</p></li></ul><h2 id="Staying-in-sync" tabindex="-1">Staying in sync <a class="header-anchor" href="#Staying-in-sync" aria-label="Permalink to &quot;Staying in sync {#Staying-in-sync}&quot;">​</a></h2><p>Two workflows keep a package aligned with the kit.</p><ul><li><p><strong>Template sync</strong> (<code>.github/workflows/template-sync.yaml</code>) re-runs <code>update</code> on a schedule and on Dependabot updates, opening or refreshing a pull request when infrastructure has drifted. A failed run opens a single <code>template-sync</code> issue instead, edited in place and closed by the next clean run. Fix a failure by re-applying the standard locally, or ask the kit for more flexibility if <code>update</code> overwrote something needed. Hand-editing the managed file does not work: the next sync reverts it. Scheduled runs also freshen reusable-workflow pins (<code>freshen_reusable_refs = true</code>): each managed caller takes the newest commit that touched the shared workflow it wraps, only ever moving a pin forwards, and skips one it cannot resolve or that floats on a branch or tag. This is the only part of <code>update</code> that needs the network, so it is off by default and on only for the scheduled run&#39;s token.</p></li><li><p><strong>Dependabot</strong> (<code>.github/dependabot.yml</code>) keeps pinned workflow and action references current, daily, grouped into one pull request per ecosystem. Slow it down by setting the <code>julia</code> ecosystem to <code>weekly</code> in your own copy.</p></li></ul><p>A fix made once in the kit reaches every adopting package on its next sync.</p><h3 id="After-a-sync,-read-your-own-docs" tabindex="-1">After a sync, read your own docs <a class="header-anchor" href="#After-a-sync,-read-your-own-docs" aria-label="Permalink to &quot;After a sync, read your own docs {#After-a-sync,-read-your-own-docs}&quot;">​</a></h3><p>&lt;!– EPIAWARE_PROSE_OK: this section names a retired tool to explain the scan, which is the one thing the scan itself cannot tell apart from drift. –&gt;</p><p>A sync converges the managed files and nothing else. Your README and the authored pages under <code>docs/src/</code> are package-owned, so prose describing the standard as it used to be survives every sync. This is the common way an adopting package ends up documenting something it no longer does: the kit moved the formatter to Runic, and a contributing guide still telling readers to run JuliaFormatter kept telling them so.</p><p><code>update</code> scans that prose for names the standard has retired (the entries in <code>RETIRED_PATHS</code>, plus tools the standard has moved away from) and reports each one in <code>warnings</code>, which the sync prints to its job log. It only reports; the wording is yours, so it never rewrites it.</p><p>The scan catches a retired name, not a stale claim. Counts, worked examples and descriptions of how a suite is organised all go stale silently, so read the pages that describe the parts of the standard the sync changed.</p><p>A page that names a retired tool in order to explain the retirement is not drift. Put <code>EPIAWARE_PROSE_OK</code> in it, in an HTML comment so it does not render, and the scan skips that file. Changelogs are never scanned, because recording what the package used to do is the point of one. That covers <code>NEWS.md</code>, <code>CHANGELOG.md</code>, and the generated <code>docs/src/release-notes.md</code> this kit writes in their place.</p><h2 id="Registration-safety" tabindex="-1">Registration safety <a class="header-anchor" href="#Registration-safety" aria-label="Permalink to &quot;Registration safety {#Registration-safety}&quot;">​</a></h2><p>The managed <code>registrability.yaml</code> caller runs the shared <code>EpiAware/.github</code> registrability workflow whenever a package&#39;s <code>Project.toml</code> changes (and on demand, and on <code>main</code>). It runs two read-only checks.</p><ul><li><p>Registrability asserts that every non-stdlib <code>[deps]</code> entry exists in the General registry with a version satisfying the package&#39;s own <code>[compat]</code>. A dependency pinned in <code>[sources]</code> to a git revision is unregistered, so the check fails with a per-dependency message. This is the failure that had ConvolvedDistributions 0.2.0 rejected from General with nothing in CI to catch it.</p></li><li><p>The reverse-dependency scan reports which org packages depend on this one and whether their <code>[compat]</code> admits the version under test. A breaking release legitimately strands a downstream bound, so this is a warning by default; set <code>fail_on_revdep_break: true</code> on the caller job to gate on it.</p></li></ul><h2 id="Unregistered-sources-pins-and-the-Julia-floor" tabindex="-1">Unregistered <code>[sources]</code> pins and the Julia floor <a class="header-anchor" href="#Unregistered-sources-pins-and-the-Julia-floor" aria-label="Permalink to &quot;Unregistered \`[sources]\` pins and the Julia floor {#Unregistered-sources-pins-and-the-Julia-floor}&quot;">​</a></h2><p><code>[sources]</code> is a Pkg 1.11 feature. Before 1.11 it is ignored without error, so Pkg resolves whatever a registry carries instead of the pin.</p><p>EpiAwarePackageTools is registered in General, so every managed environment bounds it in <code>[compat]</code> and nothing in the standard is pinned by git. A package&#39;s own <code>[compat] julia</code> is free to reach 1.10.</p><p>A package pinning a dependency of its own by git <code>[sources]</code>, such as an ecosystem sibling awaiting registration, is a different case. The kit detects such a pin from the committed environments, or takes <code>unregistered_sources = true</code> on <code>scaffold</code>/<code>update</code>, and then holds the package to Julia 1.11. It warns about a <code>[compat]</code> bound or a CI leg that reaches below it.</p><p>The <code>tests.yml</code> matrix is seeded with its <code>lts</code> leg for every other package. That leg runs <code>Pkg.test</code>, which develops the package under test itself, so the <code>[sources]</code> path pin in <code>test/Project.toml</code> being ignored costs it nothing. Only a package holding to the floor is seeded without the leg.</p><p>The jobs that run a managed environment directly rather than through <code>Pkg.test</code> — the isolated <code>test/jet</code> and <code>test/ad</code> runners, the docs build and the benchmark suite — do need their path pins honoured, so they stay on the current release. They are separate jobs, not legs of the test matrix.</p><p>Every <code>[compat]</code> bound in <code>test/Project.toml</code> has to resolve on every leg the matrix names. JET is the one to watch: it publishes nothing past 0.9.18 for Julia 1.10, so a bound starting at 0.10 makes the <code>lts</code> leg unresolvable. The seeded bound reaches back to 0.9 for that reason. <code>test/Project.toml</code> is package-owned, so a package scaffolded before this either widens the bound or drops <code>lts</code> from its own <code>julia_versions</code>.</p><p>The kit&#39;s own bound is the same kind of gate. EpiAwarePackageTools declares <code>julia = &quot;1.10, 1.11, 1.12&quot;</code> from 0.4.0 onward; 0.2.0 and 0.3.0 start at 1.11. A test environment bounded at <code>EpiAwarePackageTools = &quot;0.3&quot;</code> therefore has no resolvable version on an <code>lts</code> leg. Bump that bound before adding the leg.</p><h2 id="release-docs" tabindex="-1">Release documentation <a class="header-anchor" href="#release-docs" aria-label="Permalink to &quot;Release documentation {#release-docs}&quot;">​</a></h2><p>Documenter publishes a versioned <code>stable</code> and <code>vX.Y</code> copy of the docs only when the build runs at a release tag, and only a <code>push</code> event at that tag starts one. TagBot raises that event when a <code>DOCUMENTER_KEY</code> deploy key is set. Without the key it tags with the Actions app token, which raises no event, so the site keeps serving <code>dev</code> alone. The same fallback is refused outright when the tagged commit touches a workflow file.</p><h3 id="documenter-key" tabindex="-1">Adding the deploy key <a class="header-anchor" href="#documenter-key" aria-label="Permalink to &quot;Adding the deploy key {#documenter-key}&quot;">​</a></h3><p>Once per package, at repository creation. It needs repository admin, so <code>scaffold</code> cannot do it.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">repo</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">EpiAware/YourPackage.jl</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-keygen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rsa</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -b</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4096</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PEM</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -N</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> documenter</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/dk</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> repo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> deploy-key</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/dk.pub</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --repo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$repo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --title</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> documenter</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --allow-write</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> secret</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> DOCUMENTER_KEY</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --repo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$repo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --body</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">base64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/dk)&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shred</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/dk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/dk.pub</span></span></code></pre></div><p><code>DocumenterTools.genkeys</code> prints the same two halves to paste by hand. Four details each break the deploy silently when wrong.</p><ul><li><p>The deploy key needs <strong>write</strong> access.</p></li><li><p>The secret holds the <strong>private</strong> half, base64-encoded on one line.</p></li><li><p>One keypair per repository: GitHub refuses to reuse a deploy key, so there is no org-wide version.</p></li><li><p>RSA in PEM form, matching what <code>DocumenterTools</code> emits.</p></li></ul><p>The secret does two jobs: TagBot reads it as <code>ssh:</code> to push the tag, Documenter as <code>DOCUMENTER_KEY</code> to push to <code>gh-pages</code>. Only the tag push needs it, since <code>deploydocs</code> also works from <code>GITHUB_TOKEN</code>. That is why a keyless package still ships <code>dev</code> docs and simply never a versioned copy.</p><h2 id="Release-nudges" tabindex="-1">Release nudges <a class="header-anchor" href="#Release-nudges" aria-label="Permalink to &quot;Release nudges {#Release-nudges}&quot;">​</a></h2><p>The managed <code>release-nudge.yaml</code> caller runs the shared <code>EpiAware/.github</code> release-nudge workflow weekly and on demand. It compares <code>Project.toml</code>&#39;s version and the commits on <code>main</code> since the latest release/tag (and, best-effort, the version registered in the Julia General registry) and, whenever <code>main</code> has unreleased changes, opens or refreshes a single labelled issue that reports the released vs <code>Project.toml</code> version, how many commits are unreleased with a compare link and a short recent-commit list, and whether a version bump is still needed or only registration is outstanding. The issue spells out the next steps: comment <code>/version patch</code>, <code>/version minor</code>, or <code>/version major</code> on a pull request to bump, then either comment <code>/register</code> or run the Register workflow manually. The notes for the release are written on the GitHub release once it exists, not before it (see the <a href="/previews/PR484/getting-started/release-notes#release-notes">release notes convention</a>). When nothing is unreleased it closes any open nudge issue instead.</p><p>An open nudge issue is never edited in place. A run that finds the state has changed, or finds the issue has simply sat open too long, closes it with a short comment and opens a fresh one, so the issue is always current rather than accumulating an edit history. The generated issue body can never contain a literal <code>@</code>: the workflow strips it from any repository-derived text (commit subject lines) before it can reach the body, and names the Register workflow and the <code>/register</code> slash command instead of ever writing out a handle.</p><h2 id="How-the-kit-applies-this-to-itself" tabindex="-1">How the kit applies this to itself <a class="header-anchor" href="#How-the-kit-applies-this-to-itself" aria-label="Permalink to &quot;How the kit applies this to itself {#How-the-kit-applies-this-to-itself}&quot;">​</a></h2><p>The kit manages its own repository the same way an adopter&#39;s is managed, with one difference: it is a tooling package, so it scaffolds itself with <code>ad = false</code> (no AD CI or harness).</p><p>A <code>self-drift</code> CI check runs <code>update(&quot;.&quot;; ad = false)</code> and asserts the result is zero drift, proving the committed infrastructure matches what the templates currently produce. Because the kit is its own first adopter, this documentation site and its generated pages are the live example described in the <a href="/previews/PR484/getting-started/index#getting-started">getting-started note</a>: what you see here is exactly what the scaffold writes.</p><h2 id="Running-a-sync-by-hand" tabindex="-1">Running a sync by hand <a class="header-anchor" href="#Running-a-sync-by-hand" aria-label="Permalink to &quot;Running a sync by hand {#Running-a-sync-by-hand}&quot;">​</a></h2><p>You can drive the same sync from a Julia session:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwarePackageTools</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Re-apply the managed standard files and report drift. \`update\` is</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># \`public\`, not exported, so call it qualified.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EpiAwarePackageTools</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pkgdir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MyPackage))</span></span></code></pre></div><p><code>update</code> rewrites only the managed files and returns a manifest of what was created, updated, or preserved; package-owned files are left untouched.</p>`,63)])])}const u=t(o,[["render",n]]);export{g as __pageData,u as default};
