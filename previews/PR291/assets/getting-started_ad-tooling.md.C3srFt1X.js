import{_ as a,o as t,c as s,an as i}from"./chunks/framework.CPBG7ZiV.js";const p=JSON.parse('{"title":"AD tooling","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/ad-tooling.md","filePath":"getting-started/ad-tooling.md","lastUpdated":null}'),n={name:"getting-started/ad-tooling.md"};function r(o,e,d,l,c,h){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="ad-tooling" tabindex="-1">AD tooling <a class="header-anchor" href="#ad-tooling" aria-label="Permalink to &quot;AD tooling {#ad-tooling}&quot;">​</a></h1><p>Automatic differentiation is how the models in these packages are fit, so a gradient that is silently wrong is a serious defect. The kit ships a harness that checks each package&#39;s gradients against a trusted reference on every supported AD backend, systematically, in CI. This is the feature that most sets the kit apart from a generic package template, so this page explains both how it works and why it is built the way it is. See <a href="/previews/PR291/getting-started/infrastructure#infrastructure">Infrastructure and template sync</a> for the managed-versus-package-owned split.</p><h2 id="Why-per-backend-gradient-testing" tabindex="-1">Why per-backend gradient testing <a class="header-anchor" href="#Why-per-backend-gradient-testing" aria-label="Permalink to &quot;Why per-backend gradient testing {#Why-per-backend-gradient-testing}&quot;">​</a></h2><p>A Julia model can be differentiated by several AD backends, and they do not all agree. A backend can return a wrong gradient on a construct it mishandles while every other backend is correct, and nothing in an ordinary test suite would notice, because the model still evaluates and the sampler still runs. The only reliable check is to compute the gradient of the same log density with each backend and compare it against a reference the package trusts.</p><p>The harness makes that check systematic. Every backend runs the same scenarios against the same ForwardDiff reference, each backend runs as its own CI job so a transiently unstable backend only reds its own status, and a scenario a backend genuinely cannot handle is recorded as broken rather than quietly dropped. The result is that a regression in gradient correctness on any single backend is caught the moment it lands.</p><h2 id="Opting-in" tabindex="-1">Opting in <a class="header-anchor" href="#Opting-in" aria-label="Permalink to &quot;Opting in {#Opting-in}&quot;">​</a></h2><p>Pass <code>ad = true</code> to <code>scaffold</code> (the default for a numerical package); a tooling or non-numerical package passes <code>ad = false</code> and gets none of this.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">scaffold</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pkgdir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MyPackage); ad </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>This writes the AD CI caller, the <code>test/ad/</code> harness wiring, the <code>ADFixtures</code> registry skeleton, and the AD test environment.</p><h2 id="The-backend-matrix" tabindex="-1">The backend matrix <a class="header-anchor" href="#The-backend-matrix" aria-label="Permalink to &quot;The backend matrix {#The-backend-matrix}&quot;">​</a></h2><p>The kit tests six backends across four AD packages.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Backend</th><th style="text-align:right;">Package</th></tr></thead><tbody><tr><td style="text-align:right;">ForwardDiff</td><td style="text-align:right;">ForwardDiff</td></tr><tr><td style="text-align:right;">ReverseDiff (tape)</td><td style="text-align:right;">ReverseDiff</td></tr><tr><td style="text-align:right;">Enzyme forward</td><td style="text-align:right;">Enzyme</td></tr><tr><td style="text-align:right;">Enzyme reverse</td><td style="text-align:right;">Enzyme</td></tr><tr><td style="text-align:right;">Mooncake forward</td><td style="text-align:right;">Mooncake</td></tr><tr><td style="text-align:right;">Mooncake reverse</td><td style="text-align:right;">Mooncake</td></tr></tbody></table><p>ForwardDiff doubles as the reference: each scenario carries a ForwardDiff gradient, and the remaining backends are checked against it.</p><h3 id="One-single-source-of-truth" tabindex="-1">One single source of truth <a class="header-anchor" href="#One-single-source-of-truth" aria-label="Permalink to &quot;One single source of truth {#One-single-source-of-truth}&quot;">​</a></h3><p>The backend list is defined once in the kit, in <code>_AD_BACKENDS</code>, and everything AD-related is generated from it. That one list drives the README coverage-flag badge table, the <code>codecov.yml</code> per-backend flags and the coverage gate that waits for every flag to upload, the starter scenario test items, the AD dependency list in the scaffolded harness, and the <code>backends</code> matrix the CI caller passes to the reusable workflow.</p><p>Passing the matrix to the workflow explicitly, rather than trusting the reusable&#39;s own default, is deliberate. It means the CI matrix that actually runs can never drift from the badges and coverage flags the same package generates. Add, remove, or reorder a backend in <code>_AD_BACKENDS</code> and every one of those regenerates consistently on the next sync.</p><h2 id="The-registry-contract" tabindex="-1">The registry contract <a class="header-anchor" href="#The-registry-contract" aria-label="Permalink to &quot;The registry contract {#The-registry-contract}&quot;">​</a></h2><p>The harness has no knowledge of any particular package&#39;s types. It talks to a package&#39;s fixtures through the <a href="/previews/PR291/lib/public#EpiAwarePackageTools.ADRegistry"><code>ADRegistry</code></a> contract, so the run logic every package would otherwise copy lives in the kit while the scenarios stay in the package.</p><p>A registry is any object, commonly a package&#39;s <code>ADFixtures</code> module, that responds to:</p><ul><li><p><code>scenarios(; with_reference, kwargs...)</code> returning the gradient scenarios, each carrying its function, input, contexts, and ForwardDiff reference.</p></li><li><p><code>backends()</code> returning the backends to test as <code>(; name, backend)</code> named tuples.</p></li></ul><p>Three further accessors are optional and default to empty, so a package with no broken or skipped scenarios need not define them.</p><ul><li><p><code>broken_scenario_names()</code> for scenarios broken on every backend.</p></li><li><p><code>backend_broken_scenarios()</code> for per-backend broken scenarios.</p></li><li><p><code>backend_skip_scenarios()</code> for per-backend scenarios too unstable to run at all.</p></li></ul><p>The broken and skip bookkeeping is what lets a known gradient failure be recorded honestly. A broken scenario is still run through <a href="/previews/PR291/lib/public#EpiAwarePackageTools.check_broken"><code>check_broken</code></a>, which asserts it really does fail, so a scenario marked broken that starts passing again is flagged rather than left stale.</p><h2 id="What-the-kit-scaffolds" tabindex="-1">What the kit scaffolds <a class="header-anchor" href="#What-the-kit-scaffolds" aria-label="Permalink to &quot;What the kit scaffolds {#What-the-kit-scaffolds}&quot;">​</a></h2><p>An <code>ad = true</code> package gets:</p><ul><li><p><code>test/ad/setup.jl</code> (managed) wiring the shared harness to the package&#39;s registry and exposing <a href="/previews/PR291/lib/public#EpiAwarePackageTools.test_working_backend"><code>test_working_backend</code></a> and <a href="/previews/PR291/lib/public#EpiAwarePackageTools.test_partial_backend"><code>test_partial_backend</code></a> as thin locals.</p></li><li><p><code>test/ad/scenarios.jl</code> (package-owned) with one starter <code>@testitem</code> per backend, tagged so the per-backend CI can select a single backend by tag.</p></li><li><p><code>test/ad/runtests.jl</code> (managed) discovering those items with TestItemRunner.</p></li><li><p><code>test/ADFixtures/</code> (package-owned) the registry skeleton implementing the contract, with a placeholder scenario that runs out of the box.</p></li></ul><p>The AD items live in their own environment and their own CI, kept out of the main test run, because Enzyme, Mooncake, and the rest are heavy dependencies that the ordinary tests should not carry.</p><h2 id="Registering-scenarios" tabindex="-1">Registering scenarios <a class="header-anchor" href="#Registering-scenarios" aria-label="Permalink to &quot;Registering scenarios {#Registering-scenarios}&quot;">​</a></h2><p>Replace the placeholder scenario in <code>test/ADFixtures/src/ADFixtures.jl</code> with the package&#39;s own differentiable log densities, and add the backends the package supports to <code>backends()</code>. Group scenarios by category if the package distinguishes them, for example a marginal likelihood from a latent one, and select the category from the scenario test items.</p><p>Each backend is its own <code>@testitem</code> in <code>test/ad/scenarios.jl</code>, so a scenario group is added by writing an item that calls <code>test_working_backend</code> for the backend and category.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@testitem</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;ForwardDiff gradients (latent)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tags</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:ad</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:forwarddiff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] setup</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ADHelpers] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">begin</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    test_working_backend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ForwardDiff&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; category </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :latent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><h2 id="The-CI-workflow-and-status-badge" tabindex="-1">The CI workflow and status badge <a class="header-anchor" href="#The-CI-workflow-and-status-badge" aria-label="Permalink to &quot;The CI workflow and status badge {#The-CI-workflow-and-status-badge}&quot;">​</a></h2><p>The <code>ad.yaml</code> caller drives the per-backend matrix on pushes to <code>main</code>, pull requests, and merge queues. It skips only clearly AD-irrelevant changes such as prose docs, so anything touching sources, extensions, tests, or the suite still runs the full matrix. Each backend uploads coverage under its own flag, and the coverage gate holds the status until every flag has reported.</p><p>The kit ships one aggregate <code>ad.yaml</code> rather than six separate per-backend workflows, so the README carries a single AD status badge for the whole matrix. The per-backend detail lives in the coverage-flag table below that badge, where each backend has its own coverage badge.</p><h2 id="Running-AD-tests-locally" tabindex="-1">Running AD tests locally <a class="header-anchor" href="#Running-AD-tests-locally" aria-label="Permalink to &quot;Running AD tests locally {#Running-AD-tests-locally}&quot;">​</a></h2><p>The scaffolded <code>Taskfile.yml</code> wraps the AD runs.</p><ul><li><p><code>task test-ad</code> runs every backend in the isolated AD environment.</p></li><li><p><code>TAG=enzyme_reverse task test-ad-backend</code> runs a single backend by tag, exactly as the per-backend CI does.</p></li></ul><p>Running a single backend by tag is the fastest way to reproduce a CI failure that only one backend hits.</p><h2 id="Making-code-AD-safe" tabindex="-1">Making code AD-safe <a class="header-anchor" href="#Making-code-AD-safe" aria-label="Permalink to &quot;Making code AD-safe {#Making-code-AD-safe}&quot;">​</a></h2><p>This page is about <em>testing</em> that a package&#39;s differentiable code works across backends. Making that code differentiable in the first place — stripping an AD tape, or giving a backend an analytic derivative for a call it cannot handle, such as a <code>cdf</code> through <code>SpecialFunctions.gamma_inc</code> — is the job of <a href="https://github.com/EpiAware/EpiAwareADTools.jl" target="_blank" rel="noreferrer">EpiAwareADTools.jl</a>, the org&#39;s shared home for AD-safe evaluation hooks (<code>cdf_ad_safe</code>, <code>primal</code>, and the rest) and AD workarounds. A package imports it in its own source, and the AD scenarios registered here then exercise the result. It is a staging ground rather than a permanent home: each workaround is documented against the upstream fix meant to replace it, so entries are deleted as those land. The kit does not depend on it, and an <code>ad = true</code> package takes it on only if it needs one of those workarounds.</p>`,40)])])}const k=a(n,[["render",r]]);export{p as __pageData,k as default};
