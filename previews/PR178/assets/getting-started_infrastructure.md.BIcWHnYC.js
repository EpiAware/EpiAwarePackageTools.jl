import{_ as t,o as a,c as s,an as n}from"./chunks/framework.CLHf7yJa.js";const u=JSON.parse('{"title":"Infrastructure and template sync","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/infrastructure.md","filePath":"getting-started/infrastructure.md","lastUpdated":null}'),i={name:"getting-started/infrastructure.md"};function r(o,e,d,c,h,l){return a(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="infrastructure" tabindex="-1">Infrastructure and template sync <a class="header-anchor" href="#infrastructure" aria-label="Permalink to &quot;Infrastructure and template sync {#infrastructure}&quot;">​</a></h1><p>The kit does two jobs for an adopting package: it writes the standard infrastructure once (<code>scaffold</code>), and it keeps that infrastructure current afterwards (<code>scaffold_update</code>, driven on a schedule). This page explains the sync machinery and how the kit applies it to itself on its own repository. For what that infrastructure actually contains, see the reference pages on the <a href="/previews/PR178/getting-started/test-infrastructure#test-infrastructure">test infrastructure</a>, <a href="/previews/PR178/getting-started/benchmarking#benchmarking">benchmarking</a>, and <a href="/previews/PR178/getting-started/ad-tooling#ad-tooling">AD tooling</a>.</p><h2 id="Managed-and-package-owned-files" tabindex="-1">Managed and package-owned files <a class="header-anchor" href="#Managed-and-package-owned-files" aria-label="Permalink to &quot;Managed and package-owned files {#Managed-and-package-owned-files}&quot;">​</a></h2><p>Every file the kit writes is one of two kinds.</p><ul><li><p>Managed files are the standard infrastructure: the CI caller workflows, the documentation build (<code>docs/make.jl</code> and the VitePress theme, config, and components), the formatter and pre-commit config, and the coverage config. <code>scaffold_update</code> rewrites them from the bundled templates on every sync, so drift is removed automatically. Each managed file carries a <code>MANAGED by EpiAwarePackageTools.scaffold</code> header; do not edit them by hand.</p></li><li><p>Package-owned files are written once and never overwritten: the package&#39;s unit tests, its QA config values, the navigation tree (<code>docs/pages.jl</code>), the README body, <code>LICENSE</code>, <code>CITATION.cff</code> (your citation metadata), and the docs source pages such as this one. These are yours to edit.</p></li></ul><p>The README badge block, the README standard sections (Contributing, How to cite, Code of conduct), and the <code>.gitignore</code> standard rules are a hybrid: they are managed between markers, so their wording and the ignore rules stay current while anything you add outside the markers is preserved. The managed &quot;How to cite&quot; section points at the package-owned <code>CITATION.cff</code>, so GitHub renders a &quot;Cite this repository&quot; widget and the citation content stays yours to edit.</p><h2 id="Staying-in-sync" tabindex="-1">Staying in sync <a class="header-anchor" href="#Staying-in-sync" aria-label="Permalink to &quot;Staying in sync {#Staying-in-sync}&quot;">​</a></h2><p>Two workflows keep an adopting package aligned with the kit.</p><ul><li><p>The scheduled template-sync workflow (<code>.github/workflows/template-sync.yaml</code>) re-runs <code>scaffold_update</code> against the repository on a schedule and on Dependabot updates, then opens or refreshes a pull request whenever the committed infrastructure has drifted from the current standard.</p></li><li><p>Dependabot (<code>.github/dependabot.yml</code>) keeps the pinned reusable-workflow and action references current, so fixes in the shared workflows reach the repository without manual edits.</p></li></ul><p>An improvement made once in the kit therefore propagates to every adopting package on the next sync.</p><h2 id="How-the-kit-applies-this-to-itself" tabindex="-1">How the kit applies this to itself <a class="header-anchor" href="#How-the-kit-applies-this-to-itself" aria-label="Permalink to &quot;How the kit applies this to itself {#How-the-kit-applies-this-to-itself}&quot;">​</a></h2><p>The kit manages its own repository the same way an adopter&#39;s is managed, with one difference: it is a tooling package, so it scaffolds itself with <code>ad = false</code> (no AD CI or harness).</p><p>A <code>self-drift</code> CI check runs <code>scaffold_update(&quot;.&quot;; ad = false)</code> and asserts the result is zero drift, proving the committed infrastructure matches what the templates currently produce. Because the kit is its own first adopter, this documentation site and its generated pages are the live example described in the <a href="/previews/PR178/getting-started/index#getting-started">getting-started note</a>: what you see here is exactly what the scaffold writes.</p><h2 id="Running-a-sync-by-hand" tabindex="-1">Running a sync by hand <a class="header-anchor" href="#Running-a-sync-by-hand" aria-label="Permalink to &quot;Running a sync by hand {#Running-a-sync-by-hand}&quot;">​</a></h2><p>You can drive the same sync from a Julia session:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwarePackageTools</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Re-apply the managed standard files and report drift.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">scaffold_update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pkgdir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MyPackage))</span></span></code></pre></div><p><code>scaffold_update</code> rewrites only the managed files and returns a manifest of what was created, updated, or preserved; package-owned files are left untouched.</p>`,17)])])}const f=t(i,[["render",r]]);export{u as __pageData,f as default};
