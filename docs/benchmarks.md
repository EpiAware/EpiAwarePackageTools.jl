<!-- PACKAGE-OWNED — your benchmark narrative. scaffold writes this once and
never overwrites it. The managed `make.jl` splices this file verbatim into the
generated `docs/src/benchmarks.md`, between the page heading and the standard
"Running benchmarks" / "Performance history" sections. Replace the placeholder
prose below with commentary on what `EpiAwarePackageTools` benchmarks and how to read
the results. -->

`EpiAwarePackageTools` benchmarks its core operations to track performance over time.

Describe here what the suite covers (the operations measured, any analytical vs
numerical comparisons) and how to interpret the history below.
